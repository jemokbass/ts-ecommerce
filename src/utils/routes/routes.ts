export enum ROUTES {
  PRODUCT_DETAILS = "/product/:id",
  SEARCH = "/search",
  SEARCH_TYPE = "/search/:filterType",
  LOGIN = "/login",
  REGISTRATION = "/registration",
  RECOVERY = "/recovery",
  LOGOUT = "/logout",
  DASHBOARD = "/dashboard",
  ADMIN = "/admin",
  HOME = "/",
}

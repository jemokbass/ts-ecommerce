export enum ROUTES {
  SEARCH = "/search",
  LOGIN = "/login",
  REGISTRATION = "/registration",
  RECOVERY = "/recovery",
  LOGOUT = "/logout",
  DASHBOARD = "/dashboard",
  ADMIN = "/admin",
  HOME = "/",
}
